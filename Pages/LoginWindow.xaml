<Window x:Class="MonitoringApp.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Login Monitoring App" Height="500" Width="800" 
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize" 
        WindowStyle="None" AllowsTransparency="True" Background="Transparent">
    <Window.Resources>
        <Style x:Key="ModernInputBox" TargetType="Control">
            <Setter Property="Height" Value="45"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="#F7F9FC"/>
            <Setter Property="BorderBrush" Value="#E1E6EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Control">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Height" Value="50"/>
            <Setter Property="Background" Value="#4A90E2"/> <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="ButtonBorder" 
                                    Background="{TemplateBinding Background}" 
                                    CornerRadius="10">
                                <Border.Effect>
                                    <DropShadowEffect Color="#4A90E2" BlurRadius="15" ShadowDepth="3" Opacity="0.3"/>
                                </Border.Effect>
                            </Border>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#357ABD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CloseButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#AAB2BD"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="#FF5C5C"/> </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="20" Background="#F0F4F8">
        <Grid>
            <Button Style="{StaticResource CloseButton}" Content="✕" 
                    HorizontalAlignment="Right" VerticalAlignment="Top" 
                    Margin="0,20,20,0" Width="30" Height="30"
                    Click="BtnClose_Click"/>

            <Border Background="White" Width="400" VerticalAlignment="Center" CornerRadius="15" Padding="40">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="30" ShadowDepth="5" Color="Black" Opacity="0.1"/>
                </Border.Effect>
                
                <StackPanel>
                    <TextBlock Text="Welcome Back!" FontSize="28" FontWeight="ExtraBold" Foreground="#333" HorizontalAlignment="Center"/>
                    <TextBlock Text="Please enter your details." FontSize="14" Foreground="#999" HorizontalAlignment="Center" Margin="0,5,0,30"/>

                    <TextBlock Text="Username" Foreground="#666" FontWeight="SemiBold" Margin="0,0,0,8"/>
                    <TextBox x:Name="txtUsername" Style="{StaticResource ModernInputBox}" />

                    <TextBlock Text="Password" Foreground="#666" FontWeight="SemiBold" Margin="0,20,0,8"/>
                    <PasswordBox x:Name="txtPassword" Style="{StaticResource ModernInputBox}"/>

                    <Button Content="Login" Margin="0,35,0,0" Style="{StaticResource ModernButton}" Click="Login_Click"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>