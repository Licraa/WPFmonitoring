<UserControl x:Class="MonitoringApp.Pages.Admin.SerialMonitorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="820">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header (Back removed; use sidebar to navigate) -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,8">
            <TextBlock Text="Serial Monitor" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
            <TextBlock Text="  —  Live serial data viewer and logger" Foreground="#666" VerticalAlignment="Center" Margin="8,0,0,0"/>
        </StackPanel>

        <!-- Controls -->
        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="8" BorderBrush="#E6E9EE" BorderThickness="1" Margin="0,0,0,8">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Port:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                <ComboBox x:Name="comboPorts" Width="120" Margin="0,0,12,0"/>
                <TextBlock Text="Baud:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                <ComboBox x:Name="comboBaud" Width="100" Margin="0,0,12,0">
                    <ComboBoxItem Content="9600"/>
                    <ComboBoxItem Content="19200"/>
                    <ComboBoxItem Content="38400"/>
                    <ComboBoxItem Content="57600"/>
                    <ComboBoxItem Content="115200" IsSelected="True"/>
                </ComboBox>
                <Button x:Name="btnStart" Content="Start" Width="90" Margin="0,0,8,0" Click="BtnStart_Click"/>
                <Button x:Name="btnStop" Content="Stop" Width="90" Margin="0,0,8,0" Click="BtnStop_Click" IsEnabled="False"/>
                <Button x:Name="btnClear" Content="Clear" Width="90" Margin="0,0,8,0" Click="BtnClear_Click"/>
                <TextBlock x:Name="txtStatus" Text="Idle" VerticalAlignment="Center" Margin="18,0,0,0" Foreground="#333"/>
            </StackPanel>
        </Border>

        <!-- Log area -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="320"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" CornerRadius="8" BorderBrush="#E6E9EE" BorderThickness="1" Padding="8">
                <ListBox x:Name="listBoxLogs" FontFamily="Consolas" FontSize="13"/>
            </Border>

            <Border Grid.Column="1" Background="White" CornerRadius="8" BorderBrush="#E6E9EE" BorderThickness="1" Padding="8" Margin="12,0,0,0">
                <StackPanel>
                    <!-- Database connection section -->
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,8,0,0">
                        <Ellipse x:Name="dbStatusIndicator" Width="12" Height="12" Fill="Gray" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock x:Name="txtDbStatus" Text="DB: Disconnected" VerticalAlignment="Center"/>
                        <Button x:Name="btnDbConnect" Content="Connect DB" Width="110" Margin="12,0,0,0" Click="BtnDbConnect_Click"/>
                    </StackPanel>

                    <!-- Filtered data section -->
                    <TextBlock Text="Saving - Data" FontWeight="Bold" Margin="0,12,0,8"/>
                    <ListBox x:Name="listBoxFilteredData" FontFamily="Consolas" FontSize="13"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
