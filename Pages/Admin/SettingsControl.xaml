<UserControl x:Class="MonitoringApp.Pages.SettingsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="White">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,25">
            <TextBlock Text="System Settings" FontSize="28" FontWeight="Bold" Foreground="#1F2937"/>
            <TextBlock Text="Konfigurasi parameter sistem, database, dan jadwal kerja." Foreground="#6B7280" Margin="0,5,0,0"/>
        </StackPanel>

        <TabControl Grid.Row="1" Background="Transparent" BorderThickness="0" SelectionChanged="TabControl_SelectionChanged">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Padding" Value="20,10"/>
                    <Setter Property="FontSize" Value="14"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border Name="Border" BorderThickness="0,0,0,2" BorderBrush="Transparent" Margin="0,0,10,0">
                                    <ContentPresenter x:Name="ContentSite" ContentSource="Header" Margin="10,2"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="BorderBrush" Value="#1F2937"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>

            <TabItem Header="Database">
                <StackPanel Margin="20">
                    <TextBlock Text="Connection String"/>
                    <TextBox x:Name="txtConnectionString" Padding="8" Margin="0,5"/>
                    <Button x:Name="btnTestDb" Content="Test Connection" Click="btnTestDb_Click" Width="150" HorizontalAlignment="Left"/>
                </StackPanel>
            </TabItem>

            <TabItem Header="Shift Schedule">
                <ScrollViewer Margin="0,20,0,0">
                    <StackPanel MaxWidth="500" HorizontalAlignment="Left">
                        <TextBlock Text="Pengaturan Jam Mulai Shift" FontWeight="Bold" Margin="0,0,0,15"/>

                        <ItemsControl x:Name="icShiftList">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Margin="0,5">
                                        <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Width="150" Margin="0,0,10,0"/>
                                        <TextBox Text="{Binding StartTime, UpdateSourceTrigger=PropertyChanged}" Width="100"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <Button x:Name="btnAddShift" Content="+ Tambah Shift Baru" 
                    Background="Transparent" BorderBrush="#1F2937" BorderThickness="1"
                    Foreground="#1F2937" Padding="10" Margin="0,10,0,0"
                    Click="btnAddShift_Click"/>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <TabItem Header="Storage &amp; Logs">
                <StackPanel Margin="0,20,0,0">
                    <TextBlock Text="Folder Ekspor Excel" FontWeight="Bold" Margin="0,0,0,10"/>
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="txtExportPath" Padding="10" IsReadOnly="True" Background="#F9FAFB"/>
                        <Button Grid.Column="1" Content="Browse" Padding="15,0" Margin="10,0,0,0" x:Name="btnBrowsePath"/>
                    </Grid>
                </StackPanel>
            </TabItem>
        </TabControl>

        <Border Grid.Row="1" VerticalAlignment="Bottom" Padding="0,20,0,0" BorderThickness="0,1,0,0" BorderBrush="#E5E7EB">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="btnCancel" Content="Reset Changes" Background="Transparent" Foreground="#6B7280" BorderThickness="0" Margin="0,0,20,0" Cursor="Hand"/>
                <Button x:Name="btnSaveAll" Content="Save All Settings" Background="#1F2937" Foreground="White" Padding="25,12" FontWeight="SemiBold" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>